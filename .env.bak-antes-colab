DATABASE_URL="file:./dev.db"

# Autenticação (Fase 2) — valor local de desenvolvimento.
AUTH_SECRET="dev-local-secret-nao-use-em-producao-1234"
ADMIN_INICIAL_LOGIN="admin"
ADMIN_INICIAL_SENHA="senha123"

# --- WhatsApp em modo direto, sem n8n (testes — decisão 29) ---
# Valores locais gerados para esta máquina. Pareamento em /chat → Conexão.
WAHA_API_KEY="hSf4LjzTVzpZ8o9UHmnqluxpxQswRR6/"
CHAT_SERVICE_TOKEN="CbBuOKIzy+HeNS280lzU8ZPOMhonI9Nu"
# O app roda em container: os dois se falam pelo NOME do container, na mesma
# rede Docker. O 127.0.0.1:3001 do host não vale de dentro do container.
# Se for rodar com `npm run dev`, troque por http://127.0.0.1:3001
WAHA_URL="http://cobratec-waha:3000"

# Robô local de triagem. O Ollama roda no host e precisa ter sido iniciado com
# OLLAMA_HOST=0.0.0.0 (senão o container não alcança).
# Para desligar o robô e mandar tudo para a fila, basta apagar OLLAMA_URL.
OLLAMA_URL="http://host.docker.internal:11434"
OLLAMA_MODELO="llama3.2:1b"

# Banco de dados PostgreSQL
DB_HOST=192.168.0.253
DB_PORT=5432
DB_USER=consulta.cobratec
DB_PASSWORD=JSKc9ae6AlUzs427cKErij4y4KkLR4ji
DB_NAME=siscobraweb

# WAHA Docker Container
waha_user: admin
waha_password: edb7c7a233964df0bd22a38fb0a628aa